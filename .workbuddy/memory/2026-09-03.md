# 2026-09-03

- 为 Vercel 部署加图片代理：新增 `api/img/[[...path]].js` Edge Function（路径式 `/api/img/<host>/<path>`，域名白名单 s4.anilist.co，返回 `Access-Control-Allow-Origin: *` + 长缓存），解决跨域图片污染 canvas 无法导出的问题。
- `html/anime-grid.js`：base 从 `img-proxy.onrender.com` 改为 `/api/img/s4.anilist.co`；新增 `normalizeImageURL` 自动迁移 localStorage 里旧代理链接。
- 已验证 AniList GraphQL 角色图全部来自 s4.anilist.co；路径解析正则经 node 验证。
- 注意：api/ 在仓库根，若 Vercel 项目 Root Directory 设为 html 则需调整。
- 本地验证通过：临时 node dev server（端口 3210，模拟 Edge Function 逻辑）+ chrome-devtools MCP 实测，代理图带 crossOrigin 画入 canvas 后 toDataURL 正常导出，画布无污染。
- 按用户要求删掉旧代理（img-proxy.onrender.com，已失效）迁移代码 normalizeImageURL，loadImage 恢复直读。测试用 localStorage 已清理。
- 修复用户实测发现的 bug：存储改用 /api/img/ 相对路径后，被 drawBangumis 的 /^https/ 判断误判为文字，fillText 画出一坨 URL。修正为存储层保留 AniList 原始 https URL（canonical），drawBangumis 画图时才用 toProxyURL 转成 /api/img/s4.anilist.co。已浏览器验证图片正常绘制 + 导出。
- 经验：这个项目的 localStorage 值身兼两职（图片 URL / 自定义文字），改动存储格式必须过 /^https/ 这道判断；正确姿势是数据层 canonical、代理转换放渲染层。
- 部署确认：Vercel 项目 Root Directory 设为 html，api/ 已 git mv 进 html/api/ 使其进入构建范围，URL 路径不变（/api/img/...），零 vercel.json 配置。
- 线上 404 排查：函数其实部署成功（Resources 显示 Edge 19.7kB），但 Vercel 的 [[...path]] catch-all 文件路由在多级路径上不可靠（已知坑）。修复：改为固定文件 html/api/img.js + 查询参数 ?host=&path=，前端 toProxyURL 同步改写。已提交 2348f73，待用户 push。
- 坑记录：Vercel 部署用固定文件名 + rewrite/查询参数，避免 api 子目录下的 catch-all 动态路由；本地临时 dev server 进程可能脱离任务管理存活（taskkill 需 netstat 找 PID）。
